# Terraform
terraform/.terraform/
terraform/**/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/*.tfstate.bak.*
terraform/tfplan
terraform/tfplan.*
terraform/**/tfplan
terraform/terraform.tfvars
terraform/crash.log
terraform/override.tf
terraform/*_override.tf
# Keep terraform/.terraform.lock.hcl and bootstrap lock in git

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local tooling
node_modules/
.cache/

# Secrets / local env (portfolio-ops#57)
.env
.env.*
!.env.example
!.env.*.example
!.env.op.tpl
!.env.*.op.tpl


# Scoreboard payloads live in portfolio-ops and are published to S3 by hub GHA
# (portfolio-ops#81). Do not commit — site Deploy must not --delete wipe newer weeks.
ops/data/
ops/reports/
